import React from 'react'

const Layout = React.createClass({
  render () {
    return (
      <section className='layout'>
        this is layout
      </section>
    )
  }
})

export default Layout
